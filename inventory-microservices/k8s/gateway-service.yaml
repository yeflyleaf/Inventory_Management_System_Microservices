# ==========================================
# Gateway Service - API 网关 (对外入口)
# ==========================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  namespace: inventory-system
  labels:
    app: gateway-service
    app.kubernetes.io/part-of: inventory-management
    app.kubernetes.io/component: backend
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
        app.kubernetes.io/part-of: inventory-management
    spec:
      terminationGracePeriodSeconds: 60
      # 等待 Nacos 就绪后再启动网关
      initContainers:
      - name: wait-nacos
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -zw3 nacos-service 8848; do echo "Waiting for Nacos..."; sleep 5; done']
      containers:
      - name: gateway-service
        image: gateway-service:1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8888
          name: http
        env:
        - name: JAVA_OPTS
          value: "-Xms256m -Xmx512m -XX:+UseG1GC -XX:MaxGCPauseMillis=200"
        envFrom:
        - configMapRef:
            name: microservice-config
        - secretRef:
            name: microservice-secret
        resources:
          requests:
            cpu: 200m
            memory: 256Mi
          limits:
            cpu: "1"
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8888
          initialDelaySeconds: 60
          periodSeconds: 15
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8888
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /actuator/health
            port: 8888
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 20
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: inventory-system
  labels:
    app: gateway-service
    app.kubernetes.io/part-of: inventory-management
spec:
  type: NodePort
  selector:
    app: gateway-service
  ports:
  - protocol: TCP
    port: 8888
    targetPort: 8888
    nodePort: 30088
