# Electron 环境 API 地址模版 (C/S 模式)

# --- 基础配置 ---
# 指向后端网关地址 (例如 127.0.0.1 或 局域网 IP)
VITE_SERVER_HOST=127.0.0.1
VITE_SERVER_PORT=8888

# --- 嵌套引用 (自动组合) ---
VITE_API_BASE_URL=http://${VITE_SERVER_HOST}:${VITE_SERVER_PORT}/api
